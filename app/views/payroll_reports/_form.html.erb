<%= form_for(@payroll_report) do |f| %>
  <% if @payroll_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payroll_report.errors.count, "error") %> prohibited this payroll_report from being saved:</h2>

      <ul>
      <% @payroll_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
