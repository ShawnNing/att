<%= form_for(@slip) do |f| %>
  <% if @slip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@slip.errors.count, "error") %> prohibited this slip from being saved:</h2>

      <ul>
      <% @slip.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
